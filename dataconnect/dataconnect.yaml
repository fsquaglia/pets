specVersion: "v1beta"
serviceId: "pets"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pets-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
